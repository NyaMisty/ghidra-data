# `Parse C Source...` (CParser) Profiles

With these profiles header files can be converted to Data Type Archives (`.gdt`)
via Ghidra's CParser (`File` -> `Parse C Source...`), see [../typeinfo/gdt.md](../typeinfo/gdt.md).

